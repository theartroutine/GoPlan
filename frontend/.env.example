# Server-side BFF base URL — used by Next.js Route Handlers to call Django.
# Browser code must use BFF routes (/api/*), never call Django directly.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Browser-side WebSocket URL — browser connects directly to Django for WS.
# This is an intentional BFF bypass (see Architecture Invariants in root CLAUDE.md).
NEXT_PUBLIC_WS_URL=ws://localhost:8000
