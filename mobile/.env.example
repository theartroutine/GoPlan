# Django backend URL as reachable from the default iOS Simulator (no /api suffix).
# The Simulator shares the Mac's network stack, so localhost reaches the host service.
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000
